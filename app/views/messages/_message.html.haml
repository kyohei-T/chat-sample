.message{ value: "#{message.id}"}
  .message__user
    %p.message__user--name
      = message.user.name
    %p.message__user--timestamp
      = message.created_at.strftime("%Y/%m/%d %H:%M")
  %p.message__message
    = message.text
  = image_tag message.image, width: 200
